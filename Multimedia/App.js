import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View, SafeAreaView } from 'react-native';
import Imagenes from './componets/Imagenes';

export default function App() {
  return (
    <View  >
      <Imagenes/>
    </View >
  );
}

const styles = StyleSheet.create({
});
